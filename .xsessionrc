#!/bin/sh

[ -f ~/.xprofile ] && . ~/.xprofile
plan9start.rc

if [ "$XDG_SESSION_DESKTOP" = "awesome" ]; then
    xsettingsd &
    setxkbmap -option keypad:pointerkeys -layout us -variant altgr-intl
    nm-applet &
    pnmixer &
    xfce4-power-manager &
fi
