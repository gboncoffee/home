#!/usr/bin/env rc

SHELL=rc
PAGER=nobs
EDITOR=E
PATH=$HOME/.local/bin:$PLAN9/bin:$PATH

fn cd { builtin cd $1 && awd $sysname }

tabstop=8

plumber
$PLAN9/bin/acme -a -f $vfont -F $font
kill plumber | sh
