#!/usr/bin/env rc

#
# Start services for the Plan 9 From User Space.
#
MANPATH=$HOME/opt/plan9/man:$HOME/opt/share/man:/usr/local/man:/usr/local/share/man:/usr/share/man

if(~ `{pgrep fontsrv} '') fontsrv &
# kill a plumber already running to make sure applications can use it
slay plumber | rc
9 plumber
