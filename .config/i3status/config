#
# Gb's i3 status bar UwU <3
#

# this file should be edited as UTF-8. test:
# ß
# if the above line is not correctly displayed, fix your editor first!
# use either Emacs or Vim to have a good unicode support 👍

general {
    colors = true
    interval = 5
    color_good = "#50fa7b"
    color_degraded = "#f1fa8c"
    color_bad = "#ff5555"
}

order += "memory"
order += "disk /"
order += "cpu_temperature 0"
order += "battery all"
order += "volume master"
order += "tztime local"

memory {
    format = "Chaos: %used"
    memory_used_method = classical
}

disk "/" {
    format = "Animes: %used"
}

cpu_temperature 0 {
    format = "Coffee: %degrees °C"
    max_threshold = 55
    format_above_threshold = "Coffee: %degrees °C Too Hot!"
    path = "/sys/class/thermal/thermal_zone0/temp"
}

battery all {
    format = "Sleep: %status %percentage"
    integer_battery_capacity = true
    last_full_capacity = true
    status_chr = "CHR"
    status_bat = "BAT"
    status_unk = "UNK"
    status_full = "MAX"
    low_threshold = 20
    threshold_type = percentage
}

volume master {
    device = "pulse"
    format = "Sin wave: %volume"
    format_muted = "Sin is zeroed"
    mixer = "Master"
    mixer_idx = 0
}

tztime local {
    format = "%a %b %d, %H:%M"
}
