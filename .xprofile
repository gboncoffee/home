#!/bin/sh

# load envvars
[ -f ~/.profile ] && . ~/.profile

# set keyboard
setxkbmap -layout "us(altgr-intl)"
setxkbmap -option "caps:swapescape"

# enable touch in touchpad
xinput set-prop 14 345 1

export XDG_CURRENT_DESKTOP=LXQt
